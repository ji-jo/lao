/**
 * Soft feature gates for surfaces parked out of the current MVP.
 * Keep underlying engine / store / panel code; flip these to re-enable UI.
 */

/**
 * Path Maker — motion guides (A→B ride) + morph clips (layer A→B tween).
 * Parked for MVP 1. Timeline bars, dock chip, and paint posing stay off while false.
 */
export const PATH_MAKER_ENABLED = false;
